name: sqflite_database_flutter
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # database storage
  sqflite: ^2.0.0+3

  # 格式化时间 还有另外一个，这里用这个
  intl: ^0.17.0
  chewie: ^1.2.2
  video_player: ^2.2.3

  # display todos in grid
  flutter_staggered_grid_view: ^0.4.0-nullsafety.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/
